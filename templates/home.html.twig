{% extends 'base.html.twig' %}

{% block body %}
    <form action="{{ path('transformer') }}" method="POST" enctype="multipart/form-data">
        {{ form_widget(form) }}
        <input type="submit">
    </form>
{% endblock body %}